extends ../layout

block content
  div.well
    h1= 'Edit ' + attraction.title
    form(action='/attractions/#{attraction._id}',method='post',enctype="multipart/form-data")
      include _form
    a.btn.btn-lg.btn-default(href="/attractions")!= "Back to Attraction Feed"