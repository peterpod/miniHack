extends ../layout

block content
  div.jumbotron
    .row
      .col-xs-3    
        h1 Login
        p Already registered? Login.
      .col-xs-8.col-xs-offset-1
        form(role='form', action="/login",method="post")
          .form-group
            label.alert-danger Username :
            input.form-control(required='',type='text', name="username", placeholder='janedoeuser')
          .form-group
            label.alert-danger Password :
            input.form-control(required='',type='password', name="password")
          button.btn.btn-lg.btn-primary(type='submit') Login

  br

  div.jumbotron
    form(action='/register',method="post")
      .row
        .col-xs-3
          h1 Sign up
          p Not yet registered? Signing up is easy and free.
        .col-xs-8.col-xs-offset-1
          include ../users/_form
          .form-group
            button.btn.btn-lg.btn-primary(type='submit') Register account
