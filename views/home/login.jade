extends ../layout

block content
  div.jumbotron
    .row
      .col-xs-3    
        h1 Login
        p Already registered? Login.
      .col-xs-8.col-xs-offset-1
        form(role='form', action="/login",method="post")
          .form-group
            span Username :
            input.form-control(type='text', name="username", placeholder='janedoeuser')
          .form-group
            span Password :
            input.form-control(type='password', name="password")
          button.btn.btn-lg.btn-primary(type='submit') Login

  br

  div.jumbotron
    form(action='/register',method="post")
      .row
        .col-xs-3
          h1 Sign up
          p Not yet registered? Signing up is easy and free.
        .col-xs-8.col-xs-offset-1
          .form-group
            span Username :
            input.form-control(type="text", name="username", placeholder="janedoeuser")
          .form-group
            span Password :
            input.form-control(type='password', name="password", placeholder='')
          .form-group
            span Email :
            input.form-control(type="text", name="email", placeholder="janedoeuser@gmail.com")
          .form-group
            span First Name :
            input.form-control(type="text", name="firstname", placeholder="Jane")
          .form-group
            span Last Name :
            input.form-control(type="text", name="lastname", placeholder="Doe")
          .form-group
            button.btn.btn-lg.btn-primary(type='submit') Register account
